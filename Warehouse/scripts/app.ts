// js dependencies
import * as icons from "feather-icons";
import "../scripts/home/action";

// sass dependencies
import "../Views/app.scss";

// initialize icons
icons.replace(
    {
        "stroke-width": 1.6
    }
);
